# Codex Pulse 桌面悬浮窗 · 1.3.0

双击同目录的 **Codex Pulse.app** 即可运行。适用于 Apple Silicon Mac，macOS 14 或以上；当前已在本机 Codex 任务数据上验证。

这是独立的 macOS 桌面小工具，不需要在 Codex 插件市场中安装。可以将整个应用拖到“应用程序”文件夹。复用本机已登录的 Codex，无需额外账号或 API 密钥。任务记录留在本机；用量通过 Codex 官方接口查询，不调用模型。

## 使用

- 默认置顶，拖动标题区域即可移动。窗口宽度可以调整，高度随内容自动变化。
- 无任务时隐藏任务区，展开窗口仅显示额度内容；有任务时自动出现任务列表。任务增加时窗口向下伸长，已查看的完成任务移除后收短；达到屏幕可用高度后任务列表滚动，额度区仍保持可见。
- 顶部仅保留折叠和隐藏按钮；置顶状态可从菜单栏菜单切换。应用、悬浮窗和菜单栏使用独立的 Codex Pulse 图标：终端符号表示编程关联，蓝绿脉冲表示任务状态。向上箭头折叠为迷你状态条。
- 白色苹果风格界面，显示进行中的任务和“已完成 · 待查看”的任务，进行中的任务优先排列。完成后保留绿色标记，直到 Codex 将该任务标为已读再移除；中断或失败的任务不保留。
- 下方显示账号共享的 5 小时和每周剩余额度进度、剩余百分比与重置时间，每 30 秒更新一次（与 Codex 的“剩余”口径一致）。右下角刷新按钮可手动更新。
- 用量不可用或更新失败时明确提示，不把缺失数据当作 0%。已有数据会保留并标注“上次用量”。
- 点击任务返回对应的 Codex 任务。
- 关闭按钮仅隐藏窗口；点菜单栏波形图标可以重新显示或退出。
- 右下角菜单可以打开完成提示音，默认关闭。
- 位置及置顶偏好自动保存。支持其他桌面空间和允许辅助窗口的全屏应用；系统安全界面等仍由 macOS 控制。

## 状态含义与范围

每 2 秒读取本机 Codex 的未归档主任务，在界面展示进行中和已完成但尚未查看的任务，隐藏内部审查和子代理记录。通过当前记录文件路径识别会话存储 ID，支持对话恢复或切换记录后继续跟踪。若当前记录尚未生成或格式无法识别，会显示“状态待确认”，不回退到旧记录的完成/中断状态。下面是内部状态识别规则。

| 状态 | 含义 |
| --- | --- |
| 进行中 | 最近一轮记录为 inProgress，可能包括等待用户确认或工具返回 |
| 已完成 | 最近一轮记录为 completed；不代表整个项目目标已全部达成 |
| 已中断 | 最近一轮被中断 |
| 失败 | 最近一轮记录为 failed |
| 待确认 | 状态缺失或当前版本无法识别，绝不自动推断为完成 |

完成状态会同步 Codex 的本机未读记录。你在 Codex 点开任务查看结果后，悬浮窗通常在 2 秒内移除；为等待状态保存，刚完成的任务至少显示约 6 秒。如果完成时已在查看该任务，Codex 可能自动将结果标为已读。重启悬浮窗会保留未查看的完成状态；未读记录暂不可用或账号范围无法确定时，不会误将保留的任务当作已读。

当前版本只覆盖本机 Codex 任务，不包含 ChatGPT 普通聊天和远程主机任务。Codex 退出时显示“最后记录”提示。若 Codex 异常退出且未写入结束状态，旧记录可能仍是进行中；请在 Codex 中确认。等待授权没有独立状态。

## 数据与兼容性

应用以 SQLite 只读模式访问 `~/.codex/state_5.sqlite` 和 `~/.codex/thread_history_1.sqlite`，只查询任务标题、目录、时间和最近一轮状态，不查询对话正文、工具输出或登录凭据。另只读解析 `.codex-global-state.json` 中的任务未读标记，不修改 Codex 的数据。保留状态存储在悬浮窗自己的偏好设置中。支持通过环境变量 `CODEX_HOME` 指定数据目录。

当前 Codex 桌面进程未提供可连接的公开控制 socket，因此此版本使用本地记录适配器。它不是官方稳定集成接口；Codex 更新数据结构后可能需要更新此工具。读取失败会显示错误并保留上次记录，不会将未知状态显示成已完成。

用量客户端通过本机 Codex 可执行程序建立独立的 app-server 连接，仅发送初始化和 `account/rateLimits/read` 请求。不会启动任务、修改登录状态或兑换重置额度。退出悬浮窗时会关闭这条连接。账号需已在 Codex 中登录，并且可联网读取用量。

官方 app-server 集成参考：[OpenAI Codex App Server](https://developers.openai.com/codex/app-server)。未来若桌面版开放共享连接，可替换数据适配器而保留悬浮窗。

## 源码和验证

- `Source/TaskStore.swift`：只读任务数据适配器。
- `Source/CompletionTracker.swift`：完成保留、重启恢复及 Codex 已读状态同步。
- `Source/main.swift`：白色原生悬浮窗、菜单栏和用量进度。
- `Source/UsageStore.swift`：官方账号用量接口连接与解析。
- `build.sh`：使用 Apple Swift 编译并进行本机临时签名。
- `Tests/test_status.py`：隔离数据库测试，不修改真实 Codex 数据。

已安装 Xcode Command Line Tools 时，在此目录运行 `zsh build.sh` 重新构建，运行 `python3 -m unittest discover -s Tests -v` 执行全部测试。应用为本机临时签名版，未经 Apple 公证，面向当前电脑使用。

退出工具后删除应用即可移除；不会删除或改动 Codex 任务。
